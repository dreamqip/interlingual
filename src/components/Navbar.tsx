import {FC} from 'react';

const Navbar: FC = () => {
    return (
        <header>
            <nav className="py-6">
                <span>Translate</span>
            </nav>
        </header>
    );
};

export default Navbar;
